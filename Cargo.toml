[package]
name = "krasis"
version = "1.0.21-rc.11"
edition = "2021"
description = "LLM runtime for large MoE models with GPU prefill, GPU decode, and measured VRAM management"
license = "SSPL-1.0"

[lib]
name = "krasis"
crate-type = ["cdylib", "rlib"]

[features]
default = ["cuda"]
cuda = ["cudarc"]
gpu-debug = []  # Enable GPU decode debug instrumentation (syncs, peeks, layer capture)

[dependencies]
pyo3 = { version = "0.23", features = ["extension-module", "multiple-pymethods"] }
memmap2 = "0.9"
safetensors = "0.4"
rayon = "1.10"
half = { version = "2.4", features = ["std", "num-traits"] }
libc = "0.2"
libloading = "0.8"
log = "0.4"
env_logger = "0.11"
thiserror = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokenizers = { version = "0.21", default-features = false, features = ["onig"] }
rand = "0.8"
cudarc = { version = "0.12", features = ["driver", "cublas", "f16", "cuda-12060"], optional = true }
minijinja = { version = "2", features = ["loop_controls"] }
sha2 = "0.10"

[target.'cfg(windows)'.dependencies]
windows-sys = { version = "0.61", features = ["Win32_System_Console", "Win32_System_SystemInformation", "Win32_System_Threading"] }

[build-dependencies]
cc = "1"

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
